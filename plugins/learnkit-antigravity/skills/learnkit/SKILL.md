---
name: learnkit
description: Retrieve learned tool procedures before repeating a coding workflow.
---

# LearnKit

Use `learnkit_context` before repeating prior work. Use `learnkit_procedures` to inspect learned workflows and `learnkit_status` to diagnose the integration. Treat procedures as guidance; never bypass normal tool approvals.