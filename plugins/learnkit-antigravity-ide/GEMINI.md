# LearnKit MCP

This Antigravity IDE integration is MCP-only because the IDE does not expose the lifecycle hooks LearnKit needs for automatic native-tool capture.

Use `learnkit_context`, `learnkit_search`, and `learnkit_procedures` for retrieval. Runs are learned automatically only in hook-capable hosts or through the LearnKit SDK.