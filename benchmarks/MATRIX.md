<!-- GENERATED by `python -m benchmarks.make_results` — do not edit by hand. -->
# LearnKit — Agentic Benchmark Matrix

_Generated 2026-07-02T11:02:47.627444+00:00 from committed raw runs (`benchmarks/matrix_manifest.json`)._

**3/3 models PASS** the `playbook_effect ≥ 0.5` gate. Best quality lift **+2.25** (Llama-3.3-70B-Instruct); pooled LLM-call reduction across passing models **−38.4%**.

Run config: trials=1, k=1, seed=7, temperature=0, max_output_tokens=128.

| Model | Gate | Quality lift | react LLM calls (cold→warm) | evolution LLM calls (cold→warm) | combined | Source |
|---|---|---|---|---|---|---|
| **Qwen2.5-14B-Instruct** | ✓ PASS | +1.875 | 15 → 9 (−40.0%) | 38 → 21 (−44.7%) | 53 → 30 (−43.4%) | `results/agentic_suite_qwen2.5-14b_20260621_104149_summary.json` |
| **Qwen2.5-32B-Instruct** | ✓ PASS | +1.75 | 12 → 8 (−33.3%) | 32 → 20 (−37.5%) | 44 → 28 (−36.4%) | `results/agentic_suite_qwen2.5-32b_20260621_103650_summary.json` |
| **Llama-3.3-70B-Instruct** | ✓ PASS | +2.25 | 24 → 16 (−33.3%) | 64 → 40 (−37.5%) | 88 → 56 (−36.4%) | `results/agentic_suite_llama-3.3-70b_20260627_185537_summary.json` |

## Reproduce

Derive this table from the committed raw runs (no GPU needed):

```bash
python -m benchmarks.make_results
```

Reproduce the numbers from scratch against live model endpoints:

```bash
LK_MAX_OUTPUT_TOKENS=128 python -m benchmarks.run_agentic_matrix --trials 1 --k 1 --seed 7 --per-model-timeout 1800 --react-timeout 1500 --evolution-timeout 1500 --injection-timeout 1500 --continue-on-fail
# then:
python -m benchmarks.make_results --run
```

`react_live` measures single-shot tool-call reduction (cold vs warmed); `evolution_live` measures multi-round durability; `injection_ablation` measures the quality lift from playbook injection (the gate).
