{"id": "py01", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "My multiprocessing.Pool().map() hangs forever when I call it at module level on macOS Python 3.12. Diagnose the root cause and give me the minimal fix."}
{"id": "py02", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "On Windows, calling multiprocessing.Process(target=...).start() from a script raises 'AttributeError: Can't pickle local object'. Why and how do I fix it?"}
{"id": "py03", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "Why does my concurrent.futures.ProcessPoolExecutor deadlock on macOS when the worker function uses logging.getLogger()? Explain the cause and the standard fix."}
{"id": "py04", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "def append_item(item, items=[]): items.append(item); return items \u2014 calling this twice gives unexpected results. Explain why and the idiomatic fix."}
{"id": "py05", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "In a class, I wrote `def __init__(self, tags={}):`. Different instances are sharing tags. Why is this happening and what is the correct pattern?"}
{"id": "py06", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "My pytest fixture-less helper function `def make_config(opts={}): opts['env'] = 'prod'; return opts` corrupts subsequent test data. Explain and fix."}
{"id": "py07", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "funcs = [lambda: i for i in range(3)]; [f() for f in funcs] returns [2,2,2] instead of [0,1,2]. Why and what's the standard fix?"}
{"id": "py08", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have `handlers = [lambda evt: log(name, evt) for name in NAMES]` and every handler logs the last NAMES element. Diagnose and fix."}
{"id": "py09", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "My Tkinter loop `for i in range(5): button = Button(command=lambda: handle(i))` \u2014 every button calls handle(4). What is happening and what's the correct pattern?"}
{"id": "py10", "domain": "python_debugging", "pattern": "mixed", "prompt": "My asyncio.gather(*tasks) silently drops exceptions from individual tasks under Python 3.12. What is the actual exception-handling contract of asyncio.gather and how do I make failures visible?"}
{"id": "py11", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "I have a Python script using multiprocessing on macOS that hangs when I run it. The issue seems to be related to the default start method. Can you explain and provide a solution."}
{"id": "py12", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "On my Windows machine, using multiprocessing's Process class with a function that imports modules at the top level causes an error. What is the cause and how can I resolve it."}
{"id": "py13", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "Why does my Python script using ProcessPoolExecutor from concurrent.futures fail to work properly on macOS. Provide a detailed explanation and a fix for the issue."}
{"id": "py14", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "When using multiprocessing in a Python script on macOS, I encounter issues where the process gets stuck. Can you diagnose the problem and suggest a workaround."}
{"id": "py15", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def add_to_list(item, lst=[])` that adds an item to a list. When called multiple times, the list keeps growing unexpectedly. Why is this happening and how do I fix it."}
{"id": "py16", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "In a Python class, I have a method `def add_tag(self, tag=None)`. The tag parameter is a dictionary that keeps getting updated across different instances. Why is this happening and what is the correct approach."}
{"id": "py17", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "My Python function `def build_response(data=[])` is supposed to return a new list each time it is called. However, it returns the same list with added elements. Why is this happening and what is the correct way to handle it."}
{"id": "py18", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a list of functions created in a loop `for i in range(5): func = lambda: print(i)`. All functions print the last value of i. Why is this happening and what is the standard fix."}
{"id": "py19", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a series of event handlers created in a loop `for name in names: handler = lambda evt: log(name, evt)`. All handlers log the last name. Why is this happening and how do I fix it."}
{"id": "py20", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a Tkinter button creation loop `for i in range(5): button = Button(command=lambda: handle(i))`. All buttons trigger the handling of 4. Why is this happening and what is the correct pattern."}
{"id": "py21", "domain": "python_debugging", "pattern": "mixed", "prompt": "I have an asyncio script using gather that fails to report errors from individual tasks. What is the correct behavior of gather and how can I make sure failures are reported."}
{"id": "py22", "domain": "python_debugging", "pattern": "mixed", "prompt": "My Python script using asyncio's gather function does not raise exceptions when one of the tasks fails. How does gather handle exceptions and how can I ensure that failures are handled properly."}
{"id": "py23", "domain": "python_debugging", "pattern": "mixed", "prompt": "I have an asyncio task that uses gather to run multiple coroutines. Some coroutines fail, but the failures are not reported. What is the expected behavior of gather and how can I handle these failures."}
{"id": "py24", "domain": "python_debugging", "pattern": "mixed", "prompt": "In my Python asyncio code, I use gather to run several tasks. Sometimes, a single failure in one task prevents the entire block from completing. How does gather handle failures and how can I handle them more robustly."}
{"id": "py25", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "I have a Python script using multiprocessing that works fine on Linux but hangs on macOS. The issue seems to be related to the start method. Can you explain and provide a solution."}
{"id": "py26", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "On my Windows machine, using multiprocessing's Pool class with a function that imports modules at the top level raises an error. What is the cause and how can I resolve it."}
{"id": "py27", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "Why does my Python script using ProcessPoolExecutor from concurrent.futures fail to work properly on Windows. Provide a detailed explanation and a fix for the issue."}
{"id": "py28", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "When using multiprocessing in a Python script on Windows, I encounter issues where the process gets stuck. Can you diagnose the problem and suggest a workaround."}
{"id": "py29", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def add_to_dict(key, val, d={})` that adds a key-value pair to a dictionary. When called multiple times, the dictionary keeps growing unexpectedly. Why is this happening and how do I fix it."}
{"id": "py30", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "In a Python class, I have a method `def add_item(self, item=None)`. The item parameter is a list that keeps getting updated across different instances. Why is this happening and what is the correct approach."}
{"id": "py31", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "My Python function `def create_response(data={})` is supposed to return a new dictionary each time it is called. However, it returns the same dictionary with added keys. Why is this happening and what is the correct way to handle it."}
{"id": "py32", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def update_params(params={})` that updates a parameter dictionary. When called multiple times, the dictionary retains changes from previous calls. Why is this happening and how do I fix it."}
{"id": "py33", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a list of functions created in a loop `for i in range(3): func = lambda: print(i * 2)`. All functions print the last value of i * 2. Why is this happening and what is the standard fix."}
{"id": "py34", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def add_to_set(item, s=set())` that adds an item to a set. When called multiple times, the set keeps growing unexpectedly. Why is this happening and how do I fix it."}
{"id": "py35", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "In a Python class, I have a method `def add_to_list(self, item=None)`. The item parameter is a list that keeps getting updated across different instances. Why is this happening and what is the correct approach."}
{"id": "py36", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "My Python function `def create_object(data=[])` is supposed to return a new object each time it is called. However, it returns the same object with added elements. Why is this happening and what is the correct way to handle it."}
{"id": "py37", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def update_config(config={})` that updates a configuration dictionary. When called multiple times, the dictionary retains changes from previous calls. Why is this happening and how do I fix it."}
{"id": "py38", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a list of functions created in a loop `for i in range(4): func = lambda: print(i + 1)`. All functions print the last value of i + 1. Why is this happening and what is the standard fix."}
{"id": "py39", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a series of event handlers created in a loop `for name in ['a', 'b', 'c']: handler = lambda evt: log(name + ' processed', evt)`. All handlers log the last name. Why is this happening and how do I fix it."}
{"id": "py40", "domain": "python_debugging", "pattern": "closure_late_binding", "prompt": "I have a Tkinter button creation loop `for i in range(3): button = Button(command=lambda: handle(i * 2))`. All buttons trigger the handling of 4. Why is this happening and what is the correct pattern."}
{"id": "py41", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "I have a Python script using multiprocessing that works fine on Windows but hangs on macOS. The issue seems to be related to the start method. Can you explain and provide a solution."}
{"id": "py42", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "On my Linux machine, using multiprocessing's Process class with a function that imports modules at the top level raises an error. What is the cause and how can I resolve it."}
{"id": "py43", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "Why does my Python script using ProcessPoolExecutor from concurrent.futures fail to work properly on Linux. Provide a detailed explanation and a fix for the issue."}
{"id": "py44", "domain": "python_debugging", "pattern": "concurrency_default_start_method", "prompt": "When using multiprocessing in a Python script on Linux, I encounter issues where the process gets stuck. Can you diagnose the problem and suggest a workaround."}
{"id": "py45", "domain": "python_debugging", "pattern": "mixed", "prompt": "I have an asyncio script using gather that fails to report errors from individual tasks. What is the correct behavior of gather and how can I make sure failures are reported."}
{"id": "py46", "domain": "python_debugging", "pattern": "mixed", "prompt": "My Python script using asyncio's gather function does not raise exceptions when one of the tasks fails. How does gather handle exceptions and how can I ensure that failures are handled properly."}
{"id": "py47", "domain": "python_debugging", "pattern": "mixed", "prompt": "I have an asyncio task that uses gather to run multiple coroutines. Some coroutines fail, but the failures are not reported. What is the expected behavior of gather and how can I handle these failures."}
{"id": "py48", "domain": "python_debugging", "pattern": "mixed", "prompt": "In my Python asyncio code, I use gather to run several tasks. Sometimes, a single failure in one task prevents the entire block from completing. How does gather handle failures and how can I handle them more robustly."}
{"id": "py49", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "I have a function `def append_to_list(item, lst=[])` that adds an item to a list. When called multiple times, the list keeps growing unexpectedly. Why is this happening and how do I fix it."}
{"id": "py50", "domain": "python_debugging", "pattern": "mutable_default_arg", "prompt": "In a Python class, I have a method `def add_to_dict(self, key, value=None)`. The value parameter is a dictionary that keeps getting updated across different instances. Why is this happening and what is the correct approach."}
